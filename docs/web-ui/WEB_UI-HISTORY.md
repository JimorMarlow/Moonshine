# Moonshine Web UI — История изменений

## О проекте

Веб-интерфейс мониторинга процесса дистилляции для проекта Moonshine (Arduino).

**Особенности:**
- SVG-схема дистилляционной установки с анимациями
- Отображение датчиков температуры и потока в реальном времени
- Анимация пузырьков в нагревателе (зависит от температуры)
- Анимация капель продукта (зависит от температуры пара)
- Вращающиеся турбины в датчиках потока
- Градиентные трубы для выходной воды (цвет зависит от температуры)
- Система правил с предупреждениями и ошибками
- Симуляция работы для демонстрации

**Правила:**
- Файл `WEB_UI-HISTORY.md` можно редактировать самостоятельно без подтверждения для добавления записей об изменениях

**Последняя версия:** `qwen-distillation.037.html` (v1.1.6)

---

## История итераций

### v000 — Начальная версия
- Базовая SVG-схема дистилляционной установки
- Компоненты: Heater, Column, Deflegmator, Condenser
- Датчики температуры и потока
- Карточки сенсоров в правой панели

### v005 — v006
- Добавлены стили для компонентов
- Настроена сетка макета

### v007 — v010
- Добавлены градиенты для колонны
- Настроены цвета труб

### v011 — v015
- Добавлена анимация пузырьков в нагревателе
- Добавлена анимация капель продукта
- Настроены скорости анимаций в зависимости от температуры

### v016 — v020
- Добавлены вращающиеся турбины для датчиков потока
- Анимация потока воды с dasharray
- Предупреждающие иконки для отсутствия потока

### v021 — v025
- Добавлена система правил (Rules System)
- StatusBar с предупреждениями и ошибками
- Правила:
  - Heater > 55 AND Cool.Flow = 0 → Warning
  - Cool.T > 30 → Warning/Error
  - DF.T > 30 → Warning/Error

### v026
- Улучшена цветовая схема температур
- Градиент от синего (20°C) через зелёный к красному (30°C+)

### v027
- Финальная версия базового функционала
- Версия: v1.0.21
- Все анимации работают корректно

### v028
**Изменения:**
- Изменён путь трубы DF.Flow: теперь сначала вправо до середины дефлегматора (x=330), потом вверх
- Было: `M 280 190 L 315 190 L 315 165 L 330 165`
- Стало: `M 280 190 L 330 190 L 330 165`
- Версия обновлена: v1.0.21 → v1.0.22

### v029
**Изменения:**
- Изменён путь трубы Cool.Flow: сначала влево до середины конденсатора (x=420), потом вверх
- Было: `M 470 190 L 435 190 L 435 165 L 420 165`
- Стало: `M 470 190 L 420 190 L 420 165`
- Добавлены градиенты для вертикальных частей выходных труб
- Градиент: 20°C (синий) → текущая температура DF.T/Cool.T
- Версия обновлена: v1.0.22 → v1.0.23

### v030
**Изменения:**
- Высота SVG viewBox уменьшена: 600 → 480
- Все элементы схемы остались на тех же координатах
- Версия обновлена: v1.0.23 → v1.0.24

### v031
**Изменения:**
- Ширина SVG viewBox изменена: 700 → 480 (квадратный контейнер)
- CSS Grid: `480px minmax(250px, 1fr)`
- Все элементы на тех же координатах
- Версия обновлена: v1.0.24 → v1.0.25

### v032
**Изменения:**
- Сдвиг всей схемы на 50 единиц влево (все X-координаты -50)
- Версия обновлена: v1.0.25 → v1.0.26

### v033 ✅
**Изменения:**
- Сдвиг всей схемы ещё на 50 единиц влево (итого -100 от v031)
- Ширина SVG viewBox увеличена: 480 → 530
- CSS Grid: `530px minmax(250px, 1fr)`
- Версия обновлена: v1.0.26 → v1.0.27

### v034 ✅
**Изменения:**
- `.scheme-container`: Добавлена высота `height: 100%` и `display: flex; flex-direction: column;` для заполнения всей высоты контейнера
- `.distillation-svg`: Изменено `height: auto` → `height: 100%` для заполнения доступного пространства
- `preserveAspectRatio`: Изменено с `xMidYMid meet` на `xMidYMid slice` для масштабирования схемы с сохранением пропорций
- `.sensors-panel`: Добавлены `height: 100%`, `display: flex; flex-direction: column;`, `overflow: hidden;` для одинаковой высоты с контейнером схемы
- `.sensors-content`: Новый класс-обёртка с `flex: 1` и `overflow-y: auto` для прокрутки содержимого
- Заголовок обновлён: `Moonshine v1.0.27` → `Moonshine v1.1.3` (из `src/version.h`)
- Версия обновлена: v1.0.27 → v1.1.3

### v035 ✅
**Изменения:**
- `.status-bar`: Изменён шрифт — `font-size: 11px`, `font-weight: 400` (тонкий рубленый, как в Deflegmator/Condenser)
- Все элементы схемы сдвинуты на 5 единиц влево (все X-координаты -5)
- Добавлен `COLOR_BLENDER` — конфигурируемый массив ключевых точек `{ temp, color }` для интерполяции цвета
- Функция `getTempColor()` переписана для работы с `COLOR_BLENDER`: линейная интерполяция между ближайшими точками
- Удалена старая жёстко закодированная логика расчёта цвета
- Заголовок обновлён: `Moonshine v1.1.3` → `Moonshine v1.1.4`
- Версия обновлена: v1.1.3 → v1.1.4

### v036 ✅
**Изменения:**
- **Горизонтальный макет:** Без изменений — схема и панель сенсоров занимают 50% высоты экрана
- **Вертикальный макет** (`@media max-width: 900px`):
  - `.main-panel`: `grid-template-rows: auto auto`, `height: auto` для вертикального расположения контейнеров
  - `.scheme-container`: `height: auto`, `min-height: 480px`, `aspect-ratio: 530 / 480` — занимает всю необходимую высоту для полного отображения схемы
  - `.sensors-panel`: `max-height: 50vh` — занимает половину высоты экрана, прокрутка внутри
- Заголовок обновлён: `Moonshine v1.1.4` → `Moonshine v1.1.5`
- Версия обновлена: v1.1.4 → v1.1.5

### v037 ✅ (АКТУАЛЬНАЯ)
**Изменения:**
- **Горизонтальный макет:** Без изменений — логика работы не изменена
- **Вертикальный макет** (`@media max-width: 900px`):
  - `.sensors-panel`: `max-height: none`, `overflow: visible` — убраны ограничения высоты и прокрутка
  - `.sensors-content`: `overflow-y: visible` — таблица показывается полностью без внутренней прокрутки
  - Страница прокручивается целиком — схема видна полностью, таблица расположена ниже
- Заголовок обновлён: `Moonshine v1.1.5` → `Moonshine v1.1.6`
- Версия обновлена: v1.1.5 → v1.1.6

---

## Структура файлов

```
docs/web-ui/
├── qwen-distillation.027.html    # Базовая версия
├── qwen-distillation.028.html    # Изменена труба DF.Flow
├── qwen-distillation.029.html    # Изменена труба Cool.Flow, градиенты
├── qwen-distillation.030.html    # Высота viewBox 480
├── qwen-distillation.031.html    # Квадратный контейнер 480×480
├── qwen-distillation.032.html    # Сдвиг схемы на 50 влево
├── qwen-distillation.033.html    # Сдвиг ещё на 50, ширина 530
├── qwen-distillation.034.html    # Высота 100%, одинаковая высота панелей
├── qwen-distillation.035.html    # Шрифт статусбара, сдвиг -5, COLOR_BLENDER
├── qwen-distillation.036.html    # Адаптивный вертикальный макет
├── qwen-distillation.037.html    # ✅ АКТУАЛЬНАЯ: Вертикаль без прокрутки, v1.1.6
└── HISTORY.md                    # Этот файл
```

---

## Как продолжать работу

1. Открыть последнюю версию файла (`qwen-distillation.033.html`)
2. Внести необходимые изменения
3. Сохранить в новый файл с увеличенным номером (например, `qwen-distillation.034.html`)
4. Обновить версию в заголовке `Moonshine vX.X.X`
5. Добавить запись в этот файл

---

## Технические детали

### Датчики
| Сенсор | Описание | Диапазон |
|--------|----------|----------|
| Heater | Температура в баке | 20-95°C |
| Steam | Температура пара | 20-95°C |
| DFT | Дефлегматор (выход) | 15-40°C |
| CoolT | Конденсер (выход) | 15-35°C |
| DFFlow | Поток дефлегматор | 0-400 ml/min |
| CoolFlow | Поток конденсер | 0-400 ml/min |

### Анимации
- **Пузырьки:** 7 уровней скорости (60-90+°C)
- **Капли:** 3 уровня (1Hz, 2Hz, 4Hz при 65-70-75+°C)
- **Турбины:** 3 уровня скорости потока

### Градиенты
- **columnGradient:** Колонна (Heater → Steam)
- **vaporGradient:** Паропровод (Steam → CoolT)
- **dfOutGradient:** Выходная труба DF (20°C → DFT)
- **coolOutGradient:** Выходная труба Cool (20°C → CoolT)

### Контейнер
- **SVG viewBox:** `0 0 530 480` (ширина × высота)
- **CSS Grid:** `1fr minmax(250px, 1fr)` (схема | сенсоры)
- **Высота:** `calc(100vh - 4px)` для заполнения экрана
- **preserveAspectRatio:** `xMidYMid slice` для масштабирования с сохранением пропорций

---

*Последнее обновление: 2026-02-20*
